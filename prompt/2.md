Build the "Choose Your Tech Stack" step (Step 3 of 6) for the ContextForge project creation wizard using Next.js 14 App Router + TypeScript + Tailwind CSS + shadcn/ui.

DESIGN SYSTEM (must match existing dashboard):
- Background: #0A0A0A
- Card background: #111111
- Card border: 1px solid rgba(255,255,255,0.08)
- Border radius: 12px
- Text primary: #FFFFFF
- Text secondary: #888888
- Button border: rgba(255,255,255,0.12)
- Font: system default (Inter or Geist)

BREADCRUMB (top, full width, dark bar):
- "New Project / Create Project" — white bold for "Create Project", gray for "New Project /"
- Height 48px, border-bottom: 1px solid rgba(255,255,255,0.08)
- Same dark background as rest of page

STEP INDICATOR (below breadcrumb, full width):
- 6 steps connected by arrows (→): Basics → Features → Stack → Continuous → Design → Review
- Each step: circle with number, label below
- Active step (3 - Stack): white filled circle, white bold label
- Completed steps (1, 2): white outlined circle with number, gray label
- Upcoming steps (4, 5, 6): dark outlined circle, gray label
- Arrows between circles: gray color
- Full width, centered, padding 24px vertical
- Border-bottom: 1px solid rgba(255,255,255,0.08)

PAGE CONTENT:
- Background: #0A0A0A
- Centered content, max-width 680px, margin auto, padding 40px 0

HEADING SECTION:
- H1: "Choose Your Tech Stack" — white, bold, ~28px, centered
- Subtitle: "Select the tech stack you want to work with. AI suggestions are tailored to your case." — gray, 14px, centered, max-width 480px

STACK CATEGORY ROWS (list of cards, gap 12px, margin-top 32px):
Each row is a card (background #111111, border rgba(255,255,255,0.08), border-radius 12px, padding 20px 24px).
Row layout: [icon square] [title + description] [spacer flex-1] [dropdown] [Suggest for me button] [Not needed button]

Icon square: 40x40px, background rgba(255,255,255,0.06), border-radius 8px, centered Lucide icon (white, 18px). 
If category has a confirmed selection: show a white checkmark icon in the square instead.

Title: white, 15px, font-weight 500
Description: gray (#888), 13px, margin-top 2px

Dropdown (shadcn/ui Select):
- Width: 200px
- Background: #1A1A1A
- Border: 1px solid rgba(255,255,255,0.10)
- Text: white, 14px
- Chevron: gray

"Suggest for me" button:
- Background: transparent
- Border: 1px solid rgba(255,255,255,0.12)
- Text: white, 13px
- Hover: background rgba(255,255,255,0.06)
- Border-radius: 8px, padding: 8px 14px

"Not needed" button:
- Same as "Suggest for me" but text: #888

RENDER THESE 6 CATEGORY ROWS IN ORDER:
1. Frontend Framework | "The primary framework for your application" | Lucide: layout-grid | Dropdown value: "Expo (React Native)" | show checkmark icon (confirmed)
2. Authentication | "User authentication and authorization" | Lucide: lock | Dropdown value: "OAuth" | Suggest for me | Not needed
3. Database | "Primary database for your application" | Lucide: database | Dropdown value: "Supabase (PostgreSQL)" | Suggest for me | Not needed
4. State Management | "Client-side state management" | Lucide: box | Dropdown value: "Zustand" | Suggest for me | Not needed
5. AI Provider | "For RAG, features and AI capabilities" | Lucide: cpu | Dropdown value: "OpenAI" | Suggest for me | Not needed
6. Payments | "Payment processing and subscriptions" | Lucide: credit-card | Dropdown value: "Stripe" | Suggest for me | Not needed

BOTTOM NAV BAR (fixed bottom, full width):
- Background: #0A0A0A
- Border-top: 1px solid rgba(255,255,255,0.08)
- Height: 64px
- Left: "← Back" button (dark bg, white text, border rgba(255,255,255,0.12), border-radius 8px, padding 10px 20px)
- Right: "Continue →" button (white bg, dark text #0A0A0A, border-radius 8px, padding 10px 24px, font-weight 500)

BEHAVIOR:
- Dropdown onChange updates local React state for that category's selected value
- "Suggest for me" onClick: show a shadcn/ui Dialog/Sheet explaining this would trigger an AI suggestion call (static mock for now — show a popover with 2-3 fake suggestions to pick from, styled dark matching the rest of the UI)
- "Not needed" onClick: gray out that row (opacity 0.4) and replace the dropdown with a "Skipped" badge (gray, border-radius 6px)
- The step indicator is a shared component — import from components/wizard/step-indicator.tsx
- The breadcrumb is a shared component — import from components/wizard/breadcrumb.tsx

FILES:
- app/new-project/stack/page.tsx — main page
- components/wizard/step-indicator.tsx — shared stepper
- components/wizard/breadcrumb.tsx — shared breadcrumb
- components/wizard/stack-category-row.tsx — individual row component
- components/wizard/suggest-dialog.tsx — AI suggestion mock dialog

NO external UI libraries beyond shadcn/ui and Lucide.
All colors must be hardcoded hex/rgba values from the design system above — no Tailwind color names like gray-800, zinc-900 etc., use arbitrary values [#111111] to ensure exact color match.