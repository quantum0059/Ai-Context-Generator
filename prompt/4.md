Build the "Context Map / Dependency Graph" page for ContextForge using Next.js 14 App Router + TypeScript + Tailwind CSS + shadcn/ui + React Flow.

DESIGN SYSTEM (must match existing dashboard exactly):
- Page background: #0A0A0A
- Card/node background: #111111
- Card border: 1px solid rgba(255,255,255,0.08)
- Border radius nodes: 12px
- Text primary: #FFFFFF
- Text secondary: #888888
- All colors must use arbitrary Tailwind values [#111111] — no named Tailwind colors

---

DEPENDENCIES:
npm install reactflow
import ReactFlow, { Background, Controls, MiniMap } from 'reactflow'
import 'reactflow/dist/style.css'

---

LAYOUT:
Reuse the existing sidebar (components/dashboard/sidebar.tsx).
Main content area: flex-1, full height, flex flex-col, no scrolling (the graph fills remaining height).

---

PAGE HEADER (padding 24px 32px, border-bottom 1px solid rgba(255,255,255,0.08)):
Left side:
- Title: "Context Map / Dependency Graph" — white, bold, 22px
- Subtitle: "Visualize the relationships and dependencies between systems and services." — gray #888, 13px, margin-top 4px

Right side (flex row, gap 10px):
- "View Guides" button:
  background #111111, border 1px solid rgba(255,255,255,0.08), text white, 13px
  Lucide "sparkles" icon left (16px white), border-radius 8px, padding 8px 14px
- "Export" button:
  background #111111, border 1px solid rgba(255,255,255,0.08), text white, 13px
  Lucide "upload" icon left (16px white), border-radius 8px, padding 8px 14px

---

REACT FLOW CANVAS (flex-1, position relative, background #0A0A0A):
React Flow fills the remaining height after the header.

REACT FLOW CONFIG:
- fitView: true
- nodesDraggable: true
- nodesConnectable: false
- Background variant: dots, color rgba(255,255,255,0.06), gap 20, size 1
- MiniMap: position bottom-right, background #111111, nodeColor #333, maskColor rgba(0,0,0,0.6), style border 1px solid rgba(255,255,255,0.08)
- Controls: position bottom-right above minimap, style background #111111, border rgba(255,255,255,0.08), button color white

---

CUSTOM NODE COMPONENT (components/graph/context-node.tsx):
Each node is a dark card:
- background: #141414
- border: 1px solid rgba(255,255,255,0.10)
- border-radius: 12px
- padding: 14px 20px
- min-width: 140px
- text-align: center
- box-shadow: 0 4px 24px rgba(0,0,0,0.4)

CENTER NODE (AI Chat SaaS Platform) styling:
- background: #1A1A1A
- border: 1px solid rgba(255,255,255,0.18)
- padding: 20px 28px
- font-size: 16px font-weight 700
- min-width: 180px
- box-shadow: 0 8px 32px rgba(0,0,0,0.6)

Node content:
- Title: white, 14px, font-weight 600
- Subtitle: gray #888, 12px, margin-top 4px

---

NODES (define as initialNodes array):

id: 'center'  | label: 'AI Chat SaaS Platform' | subtitle: '' | position: { x: 380, y: 300 } | type: 'centerNode'

id: 'interface' | label: 'Interface' | subtitle: 'Frontend Application' | position: { x: 380, y: 60 }
id: 'backend'   | label: 'Backend'   | subtitle: 'Server & Logic Layer' | position: { x: 130, y: 180 }
id: 'chat'      | label: 'Chat'      | subtitle: 'AI API & Services'    | position: { x: 630, y: 180 }
id: 'portal'    | label: 'Portal'    | subtitle: 'Admin UI'             | position: { x: 60, y: 380 }
id: 'database'  | label: 'Database'  | subtitle: 'Data Store'           | position: { x: 630, y: 380 }
id: 'stripe'    | label: 'Stripe'    | subtitle: 'Payments'             | position: { x: 160, y: 520 }
id: 'openai'    | label: 'OpenAI'    | subtitle: 'AI Models'            | position: { x: 400, y: 540 }

---

EDGES (define as initialEdges array):
All edges:
- type: 'default'
- animated: true
- style: { stroke: 'rgba(255,255,255,0.20)', strokeWidth: 1.5, strokeDasharray: '5 5' }
- markerEnd: { type: MarkerType.ArrowClosed, color: 'rgba(255,255,255,0.30)' }

Connections:
- interface → center
- backend → center
- chat → center
- portal → center
- database → center
- stripe → center
- openai → center
- interface → chat
- chat → openai
- openai → database
- stripe → database
- portal → center (bidirectional: add both directions)

---

LEGEND PANEL (position: absolute, top 80px, left 24px, z-index 10):
Dark card: background #111111, border 1px solid rgba(255,255,255,0.08), border-radius 12px, padding 16px
Title: none
Items (flex col, gap 10px):
Each item: flex row, gap 8px, align-items center, font-size 13px, color #888

- Circle icon (filled gray) → "Platform"
- Hexagon icon (Lucide hexagon outline) → "Product"
- Database icon (Lucide database) → "Database"
- Diamond icon (Lucide diamond) → "Service"
- Square icon (Lucide square) → "External"
- Star icon (Lucide star) → "Feature"

---

DEPENDENCIES PANEL (position: absolute, top 80px, right 24px, z-index 10, width 220px):
Dark card: background #111111, border 1px solid rgba(255,255,255,0.08), border-radius 12px, padding 20px

Title: "Dependencies" — white, 14px, font-weight 600, margin-bottom 16px

Each dependency item (flex col, gap 14px):
Row: Lucide icon (16px white/gray) + bold label (white 13px) + description below (gray #888 12px line-height 1.4)

Items:
- monitor icon → "Interface → Chat" / "Sends prompts & displays responses"
- diamond icon → "Chat → OpenAI" / "Processes requests via AI models"
- database icon → "AI → Database" / "Stores conversations & user data"
- hexagon icon → "Payments → Database" / "Saves transactions & user plans"
- star icon → "Portal → All" / "Configures & monitors components"

---

BEHAVIOR:
- Nodes are draggable (default React Flow behavior)
- Clicking a node highlights its connected edges (use onNodeClick + setEdges to update edge styles: selected edges stroke white opacity 1, unselected gray opacity 0.2)
- Clicking canvas background resets all edges to default style
- MiniMap shows node positions, draggable viewport
- Controls: zoom in (+), zoom out (-), fit view buttons — styled dark

---

FILES:
- app/dashboard/context-map/page.tsx — main page
- components/graph/context-node.tsx — custom node component (default + center variant)
- components/graph/legend-panel.tsx — legend overlay
- components/graph/dependencies-panel.tsx — dependencies list overlay

IMPORTANT:
- Do NOT use named Tailwind colors — arbitrary values only [#111111]
- Do NOT add colored gradients or accent colors
- Do NOT add React Flow Attribution (add proOptions={{ hideAttribution: true }} to ReactFlow)
- Sidebar must be identical to existing sidebar — import and reuse, do not rebuild
- The graph canvas must fill all remaining vertical space after the header (use calc(100vh - headerHeight) or flex-1 with a flex-col parent)