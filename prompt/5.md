Build the "Prompt Editor / Viewer" page for ContextForge using Next.js 14 App Router + TypeScript + Tailwind CSS + shadcn/ui.

DESIGN SYSTEM (must match existing dashboard exactly):
- Page background: #0A0A0A
- Sidebar background: #0D0D0D
- Panel background: #111111
- Active item background: rgba(255,255,255,0.08)
- Card border: 1px solid rgba(255,255,255,0.08)
- Border radius: 12px
- Text primary: #FFFFFF
- Text secondary: #888888
- All colors must use arbitrary Tailwind values [#111111] — no named Tailwind colors

---

LAYOUT:
Reuse existing sidebar (components/dashboard/sidebar.tsx).
Main content area: flex-1, flex col.

PAGE HEADER (padding 20px 32px, border-bottom 1px solid rgba(255,255,255,0.08), text-align center):
- Title: "Prompt Editor / Viewer" — white, bold, 22px, centered full width

---

INNER LAYOUT (flex-1, display flex, flex-row, height calc(100vh - 65px)):

LEFT PANEL — PROMPT LIST (width 280px, flex-shrink 0, border-right 1px solid rgba(255,255,255,0.08), display flex, flex-col):

PANEL HEADER (padding 16px, display flex, align-items center, justify-between, border-bottom 1px solid rgba(255,255,255,0.08)):
- "Prompts" text — white, 15px, font-weight 600
- Close button: Lucide x icon inside a circle (24px, background rgba(255,255,255,0.06), border-radius 50%, border 1px solid rgba(255,255,255,0.08), cursor pointer)

SEARCH BAR (padding 12px 16px, border-bottom 1px solid rgba(255,255,255,0.06)):
- Input: full width, background rgba(255,255,255,0.04), border 1px solid rgba(255,255,255,0.08), border-radius 8px, height 36px, padding 0 12px 0 32px
- Lucide search icon: absolute left inside input, gray #888, 14px
- Placeholder: "Search prompts..." gray

PROMPT LIST (flex-1, overflow-y auto, padding 8px):
Each prompt item (padding 12px, border-radius 8px, cursor pointer, margin-bottom 2px):
- Active state: background rgba(255,255,255,0.08), border 1px solid rgba(255,255,255,0.10)
- Inactive state: background transparent, hover background rgba(255,255,255,0.04)
- Row 1: number + name — white, 14px, font-weight 500 (e.g. "03:  content-writer")
  Number styled gray #888, name styled white
- Row 2: preview text — gray #888, 13px, truncated with ellipsis, margin-top 3px

PROMPT ITEMS (8 total, item 03 is ACTIVE by default):
01 | project-requirement   | "Prepare a project brief with..."
02 | market-researcher     | "Help me do a competitor..."
03 | content-writer        | "Write a blog post on..."        ← ACTIVE
04 | code-reviewer         | "Review the following code..."
05 | data-analyst          | "Analyze this dataset and..."
06 | cold-email-writer     | "Draft a cold email to..."
07 | social-media-manager  | "Create a week of social media..."
08 | idea-generator        | "Generate creative ideas for..."

---

RIGHT PANEL — EDITOR/VIEWER (flex-1, display flex, flex-col, overflow hidden):

PANEL HEADER (padding 16px 24px, border-bottom 1px solid rgba(255,255,255,0.08), display flex, align-items center, justify-between):
- Left: active prompt filename "03:  content-writer.md" — white, 16px, font-weight 600
- Right: "Open" button — background #111111, border 1px solid rgba(255,255,255,0.08), border-radius 8px, padding 6px 14px, text white 13px, Lucide "external-link" icon left 14px

TAB BAR (padding 0 24px, border-bottom 1px solid rgba(255,255,255,0.08), display flex, gap 24px):
Two tabs: "Preview" and "Raw"
- Active tab (Preview): white text, 14px, font-weight 500, border-bottom 2px solid white, padding-bottom 10px
- Inactive tab (Raw): gray #888, 14px, no border
- Tab bar height: 42px, align-items flex-end

CONTENT AREA (flex-1, overflow-y auto, padding 24px 32px):

PREVIEW MODE renders parsed markdown with this styling:
- H1 (# Role: Content Writer):
  white, 24px, font-weight 700, margin-bottom 12px, margin-top 0
- H2 (## Overview, ## Key Guidelines, etc.):
  white, 18px, font-weight 600, margin-top 28px, margin-bottom 10px
- Paragraph text: color #CCCCCC, 15px, line-height 1.7, margin-bottom 12px
- Bullet list items:
  color #CCCCCC, 15px, line-height 1.8
  bullet style: "·" dot (not disc), margin-left 16px, list-style none, padding-left 16px with a "·" before pseudo

RENDER THIS STATIC MARKDOWN CONTENT for the active prompt (03: content-writer):

# Role: Content Writer
You are a professional content writer who creates clear, engaging, and structured content.

## Overview
Write high-quality content tailored to the audience and purpose. Follow best practices for readability and SEO.

## Key Guidelines
- Understand the topic
- Conduct research
- Organize content
- Write in a clear and engaging tone
- Edit and proofread

## Best Practices
- Use short paragraphs and simple words
- Maintain a consistent tone and voice
- Optimize for SEO without keyword stuffing
- Include examples, stats, or sources
- Ensure content is valuable and actionable

## Implementation Steps
Step 1: Gather information
Step 2: Create an outline
Step 3: Write the first draft
Step 4: Review and edit
Step 5: Publish and promote

RAW MODE renders the markdown source in a code block:
- background #0D0D0D, border 1px solid rgba(255,255,255,0.08), border-radius 8px
- padding 20px, font-family monospace, font-size 13px, color #CCCCCC, line-height 1.7
- overflow-x auto, white-space pre-wrap

---

BOTTOM ACTION BAR (height 64px, border-top 1px solid rgba(255,255,255,0.08), background rgba(255,255,255,0.04), display flex, align-items center, justify-center, gap 1px):
Two buttons side by side, separated by a 1px vertical divider (rgba(255,255,255,0.08)):

Left button (flex-1, height 100%, background transparent, border none):
- Lucide "rotate-ccw" icon, 22px, white
- Centered
- Hover: background rgba(255,255,255,0.06)
- Tooltip on hover: "Previous prompt"

Right button (flex-1, height 100%, background transparent, border none):
- Lucide "rotate-cw" icon, 22px, white
- Centered
- Hover: background rgba(255,255,255,0.06)
- Tooltip on hover: "Next prompt"

Both buttons: border-radius 0 (flush with bar)

---

BEHAVIOR:
- Clicking a prompt in the left list sets it as active, updates the right panel header and content
- Each prompt has its own static markdown content (define a prompts array with id, name, preview, content fields)
- Tab switching between Preview / Raw toggles content rendering mode
- Left panel close button (×) collapses the left panel (set width to 0, overflow hidden) — a collapsed state shows a small toggle button on the left edge to re-open it
- "Open" button in header: no-op for now, console.log the prompt id
- Previous/Next buttons in bottom bar cycle through prompts in order (rotate-ccw = previous, rotate-cw = next), wrapping at ends
- Search filters the left panel list in real time by name

---

FILES:
- app/dashboard/prompts/page.tsx — main page
- components/prompts/prompt-list.tsx — left panel
- components/prompts/prompt-viewer.tsx — right panel (preview + raw tabs)
- components/prompts/bottom-action-bar.tsx — previous/next bar
- lib/prompts-data.ts — static prompt data array

IMPORTANT:
- Do NOT use named Tailwind colors — arbitrary values only [#0A0A0A]
- Do NOT use any markdown parsing library — render markdown manually using conditional JSX based on line prefixes (# ## - etc.)
- Do NOT add any colored accents, gradients, or decorative elements
- Sidebar must be identical to existing sidebar — import and reuse, do not rebuild
- The two-panel layout must fill full height with no page scroll — only inner panels scroll independently