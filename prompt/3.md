Build the "Projects" page for ContextForge using Next.js 14 App Router + TypeScript + Tailwind CSS + shadcn/ui.

DESIGN SYSTEM (must match existing dashboard exactly):
- Page background: #0A0A0A
- Sidebar background: #0D0D0D
- Card/row background: #111111
- Card border: 1px solid rgba(255,255,255,0.08)
- Border radius cards: 12px
- Text primary: #FFFFFF
- Text secondary: #888888
- Active sidebar item: background rgba(255,255,255,0.08), border-radius 8px, white text
- All colors must use arbitrary Tailwind values [#111111] — no named colors like gray-800

---

LAYOUT:
Two-column layout, full height, no top navbar (sidebar IS the navigation):

LEFT SIDEBAR (240px fixed, height 100vh, border-right: 1px solid rgba(255,255,255,0.08)):

TOP:
- Logo row: icon (shadcn/ui or Lucide "layout-grid", 28px, white) + "ContextForge" text (white, font-weight 600, 16px). Padding 20px 16px. Border-bottom: 1px solid rgba(255,255,255,0.08).

NAV ITEMS (padding 12px 8px, flex flex-col gap 2px):
Each nav item: flex row, gap 10px, padding 10px 12px, border-radius 8px, font-size 14px, Lucide icon 18px.
Active = background rgba(255,255,255,0.08), text white.
Inactive = text #888, hover background rgba(255,255,255,0.04).

Items in order (Projects is ACTIVE):
- layout-dashboard icon → "Projects" (ACTIVE)
- layout-template icon → "Templates"
- users icon → "Members"
- grid-2x2 icon → "Templates"
- building-2 icon → "Workspaces"
- plug icon → "Integrations"
- settings icon → "Settings"
- credit-card icon → "Billing"
- book-open icon → "Documentation"

BOTTOM (margin-top auto, border-top: 1px solid rgba(255,255,255,0.08), padding 16px):
- User row: Avatar (circular, 36px, use a placeholder from https://i.pravatar.cc/36) + name "Alex Johnson" (white 13px font-weight 500) + email "alex@contextforge.com" (gray 12px) + chevron-down icon (gray)

---

MAIN CONTENT AREA (flex-1, padding 32px 32px):

PAGE HEADER:
- H1: "Projects" — white, bold, 28px, margin-bottom 4px
- Subtitle: "Manage your projects and get an overview of everything." — gray #888, 14px

TOOLBAR ROW (margin-top 24px, display flex, gap 12px, align-items center):
- Search input (flex-1, max-width 420px):
  background #111111, border 1px solid rgba(255,255,255,0.08), border-radius 8px
  height 40px, padding 0 12px 0 36px
  Lucide search icon inside left (gray, 16px)
  placeholder "Search projects..." gray
- Filter button (shadcn/ui Button variant outline):
  background #111111, border rgba(255,255,255,0.08), text white, 14px
  Lucide filter icon left, "Filter" text, chevron-down icon right
  height 40px, border-radius 8px
- "+ New Project" button:
  background #1A1A1A, border 1px solid rgba(255,255,255,0.15), text white, font-weight 500, 14px
  Lucide plus icon left
  height 40px, border-radius 8px, padding 0 16px
  hover: background rgba(255,255,255,0.08)

PROJECT LIST (margin-top 20px, flex flex-col, gap 10px):

Each project is a ROW CARD:
background #111111, border 1px solid rgba(255,255,255,0.08), border-radius 12px
padding 18px 20px
Layout: flex row, align-items center, gap 16px

LEFT: Icon square (40x40px, background rgba(255,255,255,0.06), border-radius 8px, border 1px solid rgba(255,255,255,0.08), centered Lucide icon white 18px)

MIDDLE (flex-1):
- Project name: white, 15px, font-weight 500
- Description: gray #888, 13px, margin-top 2px

RIGHT (flex row, align-items center, gap 16px):
- Status badge (see statuses below)
- Members text: Lucide users icon (gray 14px) + "X/Y" text (gray 13px)
- "Manage" text button: white 13px + chevron-down icon gray

STATUS BADGE VARIANTS (all border-radius 6px, padding 4px 10px, font-size 12px, font-weight 500):
- "In progress" → background rgba(59,130,246,0.15), text #60A5FA, border 1px solid rgba(59,130,246,0.2)
- "Completed" → background rgba(34,197,94,0.15), text #4ADE80, border 1px solid rgba(34,197,94,0.2)
- "Draft" → background rgba(255,255,255,0.06), text #888888, border 1px solid rgba(255,255,255,0.08)
- "In review" → background rgba(168,85,247,0.15), text #C084FC, border 1px solid rgba(168,85,247,0.2)

RENDER THESE 6 PROJECT ROWS:
1. Icon: bot | "AI Chat Bot Platform" | "Intelligent chatbot with NLP capabilities" | In progress | 8/12
2. Icon: shopping-cart | "E-commerce Mobile App" | "Modern mobile app for shopping experience" | Completed | 12/12
3. Icon: trending-up | "Project Dashboard" | "Analytics and metrics dashboard for businesses" | In progress | 5/8
4. Icon: database | "Task Management API" | "Backend API for task management system" | Draft | 3/4
5. Icon: sparkles | "AI Image Generator" | "Text-to-image AI generator with advanced models" | In review | 2/5
6. Icon: code-2 | "Learning Management System" | "Web platform for online courses and education" | Draft | 4/6

PAGINATION ROW (margin-top 24px, flex, justify-between, align-items center):
- Left: "Showing 1 to 6 of 6 projects" — gray #888, 13px
- Right: pagination controls using shadcn/ui Pagination:
  Prev button (chevron-left icon, dark bg, border rgba(255,255,255,0.08), 36x36px, border-radius 6px)
  Page "1" button (white bg, dark text #0A0A0A, 36x36px, border-radius 6px, font-weight 500)
  Next button (chevron-right icon, same as prev)

---

BEHAVIOR:
- Search input filters the project list in real-time by project name or description (client-side, useState)
- "Manage" chevron click: expand an inline detail panel below the row showing placeholder content (static, just a gray box with "Project details coming soon" for now)
- "+ New Project" navigates to /new-project/basics using Next.js router
- All data is static, defined as a typed array in the page component

FILES:
- app/dashboard/projects/page.tsx — main page
- components/dashboard/project-row.tsx — individual row card
- components/dashboard/sidebar.tsx — reuse or extend existing sidebar (do not duplicate)
- components/ui/status-badge.tsx — status badge with variant prop

IMPORTANT:
- Do NOT use named Tailwind colors (gray-800, zinc-900 etc.) — use arbitrary values only
- Do NOT add any colored gradients or decorative elements
- Do NOT use any third-party component libraries beyond shadcn/ui and Lucide
- Sidebar must be identical in style to the existing sidebar already built