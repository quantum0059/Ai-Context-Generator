# Optional: enables LLM-enhanced project analysis.
# When unset, the app falls back to the built-in heuristic analyzer.
LLM_PROVIDER=openai          # openai | anthropic
LLM_API_KEY=
LLM_MODEL=                   # optional, provider default used when empty
